# Menu-Based-Generic-Containers
A Project on Menu Based Generic Simple Containers which behave as the same as templates in C++ Standard Library.

The project is designed in C++. Hence use a C++ IDE to run it.
